{
  "name": "@ai-infra/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "files": [
    "**/*.ts",
    "!**/*.test.ts",
    "!**/*.spec.ts"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no linter configured yet'"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
